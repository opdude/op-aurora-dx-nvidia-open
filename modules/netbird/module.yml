name: netbird
description: Install NetBird VPN client
author: local
version: 1.0.0

# Module type
type: script

# Default configuration
defaultOptions:
  xdg_current_desktop: "KDE"

# Required options schema
options:
  xdg_current_desktop:
    type: string
    description: Name of the desktop environment (e.g., KDE, GNOME)
    required: false